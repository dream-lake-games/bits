apiVersion: rbac.authorization.k8s.io/v1
kind: RoleBinding
metadata:
  name: bits-lobby
  namespace: {{ .Values.namespace }}
subjects:
  - kind: ServiceAccount
    name: bits-lobby
    namespace: {{ .Values.namespace }}
roleRef:
  kind: Role
  name: bits-lobby
  apiGroup: rbac.authorization.k8s.io

