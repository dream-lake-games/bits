# Functional State

The primary goal today is to wrap up the state of a room. I believe the server state is good, just need to finish the derived host + client states, and associated v0 UI.

It would then I think be good to properly refactor the abstractions a bit to support:

- Client as host
- Server-hosted AIs

By the end of the day it would be awesome if I could run the server headless (no rendering), have a single client join, add an AI to play against, and have a funcitoning game.

Then tomorrow I could look into actually hosting it somewhere.
