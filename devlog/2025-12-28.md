# Getting it workign e2e

Where was I at yesterday? If I recall correctly I liked what I did on the client side with state and I wish I had something similar for the server. What I was tring to do with the server was silly.

I think it's reasonable to have "host state" in addition to client state. I actually think this is really good, it'll then make it really easy to divorce host from server later on

Yeah I think this actually what I should do. I should make a nice "host" module, that is entirely derived from state.

## Here is how I want to refactor things before continuing

DECISION: Don't worry about the host thing yet, but try to define things in a reasonable way so it's easy later

- server_lobby — responsible for getting the player connection, name input and handling it correctly
- QUIRK: also exports the "start game" system which we'll use for now in the host code. Eventually this will be another client input.

- server_state - i don't think this should exist, in favor of...
- server_game - responsible for processing the client input and updating all of the components
- server_question - i like how it is, simple abstraction for question fetching

Then we'll introduce the "host" module, which will have:

- host_state - basically the same as client state. i think the only state's we'll care about are:
  - lobby
  - waitingforquestion
  - answering
  - betting
  - reviewing
- host_game — this is the UI stuff, what is currently bleeding into a bunch of other stuff

## What is work?

Is this something I want to do? Am I good at this? Does it bring me joy...?

Hopefully
